---
- name: Install docker and docker-compose
  pacman:
    name:
      - docker
      - docker-compose
    state: present
    update_cache: yes

- name: Create file dir
  file:
    path: /opt/files
    state: directory
    mode: '0755'

- name: Create text files
  copy:
    content: "Text file {{ item }} by Ansible\n"
    dest: "/opt/files/test{{ item }}.txt"
  loop: [1, 2, 3]

- name: Copy nginx.conf
  template:
    src: nginx.conf.j2
    dest: /opt/nginx.conf

- name: Run nginx docker
  docker_container:
    name: nginx-files
    image: nginx:alpine
    state: started
    restart_policy: always
    ports:
      - "8080:80"
    volumes:
      - /opt/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /opt/files:/usr/share/nginx/html/files:ro